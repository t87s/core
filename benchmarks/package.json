{
  "name": "@t87s/benchmarks",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench --run",
    "bench:watch": "vitest bench"
  },
  "dependencies": {
    "@t87s/core": "file:..",
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
